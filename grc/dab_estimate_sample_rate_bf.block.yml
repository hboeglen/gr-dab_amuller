# auto-generated by grc.converter

id: estimate_sample_rate_bf
label: DAB Est Samp Rate
category: '[DAB]'

parameters:
-   id: exp_samp_rate
    label: Samp. Rate
    dtype: int
    default: samp_rate
-   id: frame_length
    label: Frame Len.
    default: 0
    dtype: int

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: float

templates:
    imports: from gnuradio import dab
    make: dab.estimate_sample_rate_bf(${exp_samp_rate}, ${frame_length})

file_format: 1
